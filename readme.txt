只能运行在windows系统下，使用windows的gdi绘图接口作为显示层。

[使用说明]
需要先编译"Engine"项目，生成必要的库文件，然后再选择"Playground"项目作为启动项运行

做了以下工作
a.深度缓存
b.背面裁剪
c.自定义顶点、像素着色器
d.透视正确的顶点属性插值

------
以下简要的提下怎样开始写一个简易软件光栅化程序

1.分配一块"宽x高x像素(每像素字节数, byte)"的内存块，用于保存绘制状态。

2.怎样画一条直线？

3.怎样把本地空间(模型空间)的顶点位置投影到齐次裁剪空间？理解[模型] -> [观测] -> [投影(正交/透视)]变换，这步很重要。

4.将齐次裁剪空间的顶点位置映射到屏幕，也称为视口变换。

5.如何填充在屏幕上的由3个顶点(位置)连接成的三角形？